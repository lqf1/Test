my: 
  name: jack
  age: 22
  number: ${random.int}
  uuid: ${random.uuid}
  max: ${random.int(10)}
  value: ${random.value}
  greeting: hi ${my.name}
---  
spring:
 profiles: dev
name: Everyone  
--- 
spring: 
 profiles: hello
name: ddd
---
spring:
  profiles:
    active: dev
---

server:
  port: 8081
spring:
  profiles:
    active: hello
management:
  security:
   enabled: false
  port: 8888
  context-path: /test
  
endpoints:
  actuator:
    enabled: true
  shutdown:
    enabled: true
    
info:
  app: 
     name: spring-boot-actuator
     version: 1.0.0